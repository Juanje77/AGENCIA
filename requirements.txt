# Dependencias del despliegue. Vercel instala este archivo automaticamente.
#
# El nucleo del sistema usa solo la biblioteca estandar de Python: estas
# habilitan la lectura de facturas.

pymupdf>=1.24      # facturas en PDF
openpyxl>=3.1      # planillas de liquidacion en Excel
anthropic>=0.40    # leer con IA las facturas que el lector normal no interpreta

# OCR para facturas escaneadas, alternativa gratuita a la IA. No se puede usar
# en Vercel porque necesita el motor Tesseract instalado en el sistema
# operativo; si vas a correr el sistema en una computadora, descomentalo e
# instala tambien:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   Mac:     brew install tesseract tesseract-lang
#   Linux:   apt install tesseract-ocr tesseract-ocr-spa
# pytesseract>=0.3
# pillow>=10.0
